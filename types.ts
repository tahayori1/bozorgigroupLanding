export interface Product {
  nameKey: string;
  imageUrl: string;
  descriptionKey: string;
}

export interface FamilyMember {
  nameKey: string;
  titleKey: string;
  imageUrl: string;
  descriptionKey: string;
  generation: number;
}